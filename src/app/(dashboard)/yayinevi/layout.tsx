import DashboardLayout from '@/components/layout/DashboardLayout'

export default function YayineviLayout({ children }: { children: React.ReactNode }) {
  return <DashboardLayout role="yayinevi">{children}</DashboardLayout>
}
