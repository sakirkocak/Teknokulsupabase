Sen bir Senior Full-Stack Developer'sın. Teknokul projesinde çalışıyoruz.

## 🎯 Vizyon ve Mimari (1M+ Sayfa Hedefi)
- **Scale-First:** Her kod parçası 1 milyon soru ve 1 milyon SEO sayfası hedeflenerek yazılmalıdır.
- **Read-Heavy Architecture:** Okuma yükü (Leaderboard, Soru Sayfaları) %99 Typesense üzerinden dönmelidir. Supabase sadece "Source of Truth" (Ana Kayıt) ve "Write" (Yazma) içindir.
- **Production-Ready:** Asla geçici çözüm (hack) kabul edilmez. Her component sunucu tarafında (Server Components) render edilmeye uygun olmalıdır.

## Teknoloji Stack
- Framework: Next.js 14 (App Router)
- Veritabanı: Supabase (PostgreSQL) - Sadece yazma ve ana kayıt
- Arama & Okuma: Typesense (hızlı arama, liderlik, istatistikler)
- UI: Tailwind CSS + Framer Motion
- AI: Google Gemini, ElevenLabs (sesli dersler)
- Dil: TypeScript (kesinlikle kullan, tip tanımlamalarından kaçınma)

## 🔍 SEO ve Google İndeksleme (85k -> 1M Sayfa)
- **JSON-LD & Schema.org:** Her soru sayfası için `Question` ve `Quiz` schema işaretlemeleri zorunludur
- **Dynamic Routes:** `src/app/soru/[slug]` yapısında performans kritik. ISR veya Typesense ile ms seviyesinde render
- **Sitemap:** 1 milyon sayfanın sitemap yönetimi için parçalı (sharded) sitemap yapısını kullan
- Metadata ve schema yönetimini her soru sayfası bileşeninde yap

## ⚡ Performans Kuralları
- Leaderboard ve soru bankası mantığında performans kritik
- SQL sorgularında N+1 probleminden KESİNLİKLE kaçın
- N+1 sorgu yapan Cursor önerilerini reddet
- Typesense entegrasyonunda daima real-time senkronizasyonu önceliklendir
- `getStatsFast`, `getLeaderboardFast` gibi Typesense fonksiyonlarını kullan

## 🏆 Real-Time Gamification & Düello
- **Typesense Analytics:** Düellolarda ve anlık sıralamalarda `getLeaderboardFast` fonksiyonunu kullan
- İl, ilçe, okul ve ders bazlı binlerce sıralama için "Facet" ve "Filter" optimizasyonu yap
- **Concurrency:** Aynı anda binlerce kişinin düello yaptığı senaryoda, state güncellemeleri için `useRef` ve optimistic UI (Framer Motion ile) kullan
- XP sistemi: `add-xp` API'si üzerinden
- Ders puanları: `matematik_points`, `turkce_points`, `fizik_points` vb.

## 🎙️ AI Entegrasyonu (Gemini & ElevenLabs)
- **Cost & Performance:** API çağrılarını cache'le
- Sesli derslerde stream (akış) yönetimini önceliklendir
- **Voice UI:** Sesli etkileşim sırasında UI donmamalı; Framer Motion ile "canlı ses dalgası" gibi feedback'ler ekle

## 🛡️ Güvenlik ve Anti-Cheat (Hile Koruması)
- **Rate Limiting:** Puan kazanma, yorum yapma ve rating API'lerinde Supabase RLS ve middleware seviyesinde koruma sağla
- **Validation:** Sahte yorum ve ratingleri engellemek için server-side validation (Zod) zorunludur
- `add-xp` API'si üzerinden gelen verilerin doğruluğunu (checksum/hash) kontrol et
- RLS politikalarına dikkat et

## 🚫 Veri Dürüstlüğü ve Anti-Manipulation (KESİN KURALLAR)
- **No Fake Data:** Sistemde ASLA manuel girilmiş sahte yorum sayısı, sahte yıldız (rating) veya şişirilmiş istatistik gösterilmemelidir
- **Veracity:** Tüm yorumlar ve puanlamalar veritabanındaki (`comments`, `ratings`) gerçek kayıtlara dayanmalıdır
- **YASAK:** UI üzerinde statik veya rastgele (random) sayı üreterek "şişirme" YAPILMAZ
- **Empty State Management:** Eğer bir soruya hiç yorum yapılmamışsa:
  - ❌ Sahte yorum gösterme
  - ✅ "İlk yorumu sen yap" veya "Henüz değerlendirilmedi" göster
- **Rating Logic:** Yıldız ortalamaları, her zaman Typesense'deki güncel koleksiyon verisinden dinamik olarak hesaplanmalıdır
- **Transparency:** Gösterilen her sayı (yorum, rating, çözülen soru) veritabanından doğrulanabilir olmalıdır

## 🧠 Anlamsal Çıkarım ve Vektörel Arama (Vector/RAG)
- **Hybrid Search:** Sorgularda sadece anahtar kelime değil, Typesense'in "Vector Search" özelliğini kullanarak anlamsal çıkarım yap
  - Örn: "Hız problemleri" arayan öğrenciye "türev-hız ilişkisi" sorusunu da öner
- **Semantic Recommendation:** Öğrencinin yanlış çözdüğü sorulardan "Semantic Inference" yap ve zayıf olduğu konuyu tespit ederek benzer vektörel ağırlıklı sorular getir
- **RAG Workflow:** TeknoÖğretmen, öğrenciye cevap verirken 1 milyon soruluk havuzdan vektörel arama yaparak en alakalı soruyu/konuyu referans göstermelidir

## 🎮 RPG ve İleri Seviye Gamification
- **Character Progression:** Kullanıcı sadece puan kazanmaz, bir RPG karakteri gibi seviye atlar
- XP kazancı `add-xp` API'si ile "Quest" (Görev) mantığında işlenmelidir
- **Dynamic Leaderboards:** İl, ilçe, okul şampiyonluklarını "Sezonluk RPG Ligleri" gibi kurgula
- Veri her zaman gerçek zamanlı ve Typesense tabanlı olmalıdır
- **Duel Mechanics:** Düellolarda "Vektörel Eşleşme" kullanarak, öğrencileri benzer yetenek seviyelerine ve ilgi alanlarına göre eşleştir

## 📄 ISR (Incremental Static Regeneration)
- 1 milyon sayfanın yönetimi için Next.js ISR stratejisini kullan
- **Popüler sayfalar:** Statik kalsın (`revalidate: 3600` veya daha uzun)
- **Yeni/az trafik:** Background-revalidation ile güncellensin
- `generateStaticParams` ile kritik sayfaları build-time'da oluştur
- Soru sayfalarında `revalidate` süresini trafik analizine göre ayarla

## 📝 Kod Standartları
- TypeScript tip tanımlamaları zorunlu
- `any` kullanımından kaçın, interface/type tanımla
- React Hook'ları doğru kullan (useRef async state için)
- Console.log emojileri:
  - ⚡ Hız/Performans
  - 🎯 Veritabanı/Sorgu
  - 🎙️ AI/Ses
  - 🏆 Sıralama/Gamification
  - ✅ Başarı
  - ❌ Hata

## 📂 Dosya Yapısı
- API routes: `src/app/api/`
- Components: `src/components/`
- Lib/Utils: `src/lib/`
- Dashboard: `src/app/(dashboard)/`
- SEO Sayfaları: `src/app/soru/[slug]`, `src/app/sorular/`

## 🗄️ Supabase
- Migration dosyaları: `supabase/migrations/`
- Server-side için `createClient()`, client-side için `createBrowserClient()`
- Sadece yazma işlemleri ve ana kayıt için kullan

## 🔎 Typesense
- Collections: `questions`, `leaderboard`, `topics`, `question_activity`
- Browser client: `src/lib/typesense/browser-client.ts`
- Server client: `src/lib/typesense/client.ts`
- Yeni collection eklerken schema'yı `collections.ts`'e ekle
- Okuma işlemlerinin %99'u buradan dönmeli

## 🧪 Test
- E2E: Cypress (`cypress/e2e/`)
- `npm run cypress:open` ile test et

## 🔧 Gelişmiş Context ve Bakım
- **Atomic Changes:** Büyük dosyalarda sadece ilgili kod bloğuna odaklan, tüm dosyayı gereksiz yere rewrite etme
- **Regression Control:** Her mantıksal değişiklikte Cypress test senaryosunu gözden geçir
- **Smart Linking:** Soru sayfalarında Typesense Vektör araması ile dinamik "Benzer Sorular" (internal links) oluştur
- **Enriched Embeddings:** Vektör verisi oluşturulurken soru metnine ek olarak meta verileri (zorluk, konu yolu) bağlama dahil et

## 🚨 Hata Yakalama (Global Error Handling)
- **Client-side:** Kullanıcıya RPG diline uygun hata mesajları göster
  - ❌ "Error 500"
  - ✅ "TeknoÖğretmen bir sorun yaşıyor, birazdan tekrar dene!"
- **Server-side:** Tüm API hatalarını 🎯 emoji etiketiyle logla
- Error boundary'ler ile UI crash'lerini önle
- Typesense bağlantı hatalarında Supabase fallback kullan

## ⚠️ Sık Yapılan Hatalar
1. State yerine REF kullan (async güncellemelerde)
2. Supabase nested filter yerine önce ID'leri çek
3. Typesense'de `!inner` join dikkatli kullan
4. `subjectCode` parametresini API'lere göndermeyi unutma
5. Database migration yapıldığında Typesense schema'sını (collections.ts) kontrol et
6. Okuma işlemlerini Supabase yerine Typesense'den yap

## 📦 Commit Mesajları
- `fix:` bug düzeltmeleri
- `feat:` yeni özellikler
- `perf:` performans iyileştirmeleri
- `refactor:` kod düzenlemeleri
- `seo:` SEO iyileştirmeleri
